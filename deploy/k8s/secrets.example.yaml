# Example secrets — do NOT commit the real one. Apply with:
#   kubectl -n dev-thriller create secret generic dt-secrets --from-env-file=apps/backend/.env
# or convert with `kubectl create secret generic dt-secrets --from-literal=...` per key.
#
# This file is a reference. The keys below MUST match env.ts expectations.

apiVersion: v1
kind: Secret
metadata:
  name: dt-secrets
  namespace: dev-thriller
type: Opaque
stringData:
  MONGO_URI: "mongodb+srv://USER:ROTATED_PASSWORD@cluster.mongodb.net/?retryWrites=true&w=majority"
  MONGO_DB: "devthriller"
  JWT_ACCESS_SECRET: "rotate-me-64hex"
  JWT_REFRESH_SECRET: "rotate-me-64hex"
  R2_ACCOUNT_ID: "31193190e8f060cc2b27cdd6b01b5ec0"
  R2_ACCESS_KEY_ID: "ROTATE-ME"
  R2_SECRET_ACCESS_KEY: "ROTATE-ME"
  R2_BUCKET: "smartxalgo"
  SMTP_HOST: "smtp.example.com"
  SMTP_PORT: "587"
  SMTP_USER: "postmaster@example.com"
  SMTP_PASS: "ROTATE-ME"
  SMTP_SECURE: "false"
  SMTP_FROM: "Dev Thriller <no-reply@devthriller.app>"
  FRONTEND_ORIGIN: "https://app.example.com"
